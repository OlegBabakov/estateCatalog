{% extends "FOSUserBundle::layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block fos_user_content %}
    <p>{{ 'registration.confirmed'|trans({'%username%': user.username}) }}</p>
    {% if targetUrl %}
    {#<p><a href="{{ targetUrl }}">{{ 'registration.back'|trans }}</a></p>#}
    {% endif %}
    <div class="row">
        <div class="col-md-3"></div>
        <div class="col-md-6">
            {#<h4><b>Начните работать уже сейчас</b></h4>#}
            <a class="btn btn-success m-t-1" href="{{ path('root') }}">
                {{ 'filter_form_search'|trans({},'messages') }} <i class="fa fa-search fa-fw" aria-hidden="true"></i>
            </a>
            <a class="btn btn-success m-t-1" href="{{ path('cabinet_mainpage') }}">
                {{ 'user_account'|trans({},'messages') }} <i class="fa fa-user" aria-hidden="true"></i>
            </a>
            {#<a class="btn btn-success m-t-1" href="{{ path('resume_add') }}">Создать резюме</a>#}
            {#<a class="btn btn-success m-t-1" href="{{ path('user_homepage') }}">Личный кабинет</a>#}
        </div>
        <div class="col-md-3"></div>
    </div>
{% endblock fos_user_content %}
